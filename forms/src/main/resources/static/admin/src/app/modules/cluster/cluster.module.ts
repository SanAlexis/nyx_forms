import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

@NgModule({
  declarations: [
	cluster-tree,
	cluster-detail,
	cluster-id-setting,
	cluster-buttons
	],
  imports: [
    CommonModule
  ]
})
export class ClusterModule { }
